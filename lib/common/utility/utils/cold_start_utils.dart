abstract class ColdStartUtils {
  static bool isAppInit = false;

  // 0 or Only 1 of the following variables is not null

  // If a variable is not null, it will be handle in onInit
  // of SplashPage

  static String? scanCodeUrl;
}
