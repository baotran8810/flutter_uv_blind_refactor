import 'package:flutter_uv_blind_refactor/views/modules/pages/setting_page/setting_page_controller.dart';
import 'package:get/get.dart';

class SettingPageControllerImpl extends GetxController
    implements SettingPageController {
  // final SettingController _settingController;

  // SettingPageControllerImpl({
  //   required SettingController settingController,
  // }) : _settingController = settingController;

  // @override
  // Future<void> setUseCustomSemantics(bool newValue) async {
  //   bool isConfirmed = true;

  //   if (newValue) {
  //     isConfirmed = await DialogViewUtils.showConfirmDialog(
  //       messageText: tra(LocaleKeys.txt_useCustomSemanticsWarning),
  //     );
  //   }

  //   if (isConfirmed) {
  //     _settingController.setUseCustomSemantics(newValue);
  //   }
  // }
}
